specVersion: "v1beta"
serviceId: "FirebaseAuth"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "FirebaseAuth-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
